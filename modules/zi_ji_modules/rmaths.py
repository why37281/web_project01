# todo
def shus_che(x, y):
    enter = (len(str(x * y)) + 2) * '—'
    total = None
    a = (len(str(x * y)) - len(str(x))) * ' ' + str(x)
    b = (len(str(x * y)) - len(str(y))-2) * ' ' + str(y)
    for i in range(len(str(y))):
        if i == 0:
            total = (y % 10) * x
        else:
            total =
        c =
    d =
    e =
    f =
    i = a + b + enter + c + d + e + f
    return i
print(shus_che(135, 24))